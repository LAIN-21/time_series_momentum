# Research

| Artifact | Role |
|----------|------|
| `03_disagreement.ipynb` | Agreement buckets vs next-month chop |
| `04_robustness.ipynb` | OOS / no-BTC / cost ladder / vol target |
| `05_combo_grid.ipynb` | Weights / subsets / raw avg / majority bake-off |
| `combo_grid_*.csv` / `combo_grid_summary.json` | Combo rankings for content |
| `memos/` | One-page claim sheets for each chapter |
| `results_snapshot.json` | Latest numeric snapshot for content |

Refresh snapshot from repo root:

```bash
./venv/bin/python scripts/refresh_snapshot.py
```
